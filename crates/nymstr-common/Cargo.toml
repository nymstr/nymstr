[package]
name = "nymstr-common"
version = "0.1.0"
edition = "2021"

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
fern = { version = "0.6", features = ["colored"] }
log = "0.4"
anyhow = "1.0"
